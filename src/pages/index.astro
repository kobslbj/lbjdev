---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
  
const pageTitle = "Justin's Blog";
const allPosts = await getCollection('posts');
const latestPosts = allPosts
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime())
  .slice(0, 4);
---

<BaseLayout pageTitle={pageTitle}>
  <div class="chat-container max-w-4xl mx-auto flex flex-col mb-8">
    <!-- Chat Messages Area -->
    <div id="chatMessages" class="chat-messages py-6 space-y-4 mt-6">
      <div class="message bot-message flex gap-3">
        <div class="avatar w-10 h-10 rounded-full overflow-hidden border-2 flex-shrink-0">
          <img src="/images/justinCover.jpg" alt="Justin" class="w-full h-full object-cover" />
        </div>
        <div class="message-content bg-[#1C2128] rounded-2xl rounded-tl-sm px-5 py-3 border border-[#2A2D32]">
          <p class="text-[#E6EDF3]">嗨！我是 Justin，目前正在創業，夢想是打造能讓世界變得更好的產品。我目前正在創業的路上，如果你也在創業或對軟體開發有興趣，歡迎一起交流想法！</p>
        </div>
      </div>
            <div class="message bot-message flex gap-3">
        <div class="avatar w-10 h-10 rounded-full overflow-hidden border-2 flex-shrink-0">
          <img src="/images/justinCover.jpg" alt="Justin" class="w-full h-full object-cover" />
        </div>
        <div class="message-content bg-[#1C2128] rounded-2xl rounded-tl-sm px-5 py-3 border border-[#2A2D32]">
          <p class="text-[#E6EDF3]">AI時代持續產出很重要，寫Blog是一個很好的方式，能幫助我思考，也能跟大家一起學習。</p>
        </div>
      </div>
    </div>
  </div>
  <!-- Latest Blog Cards -->
  <section class="mt-6">
    <div class="flex items-end justify-between mb-6">
      <h2 class="text-2xl sm:text-3xl font-semibold text-[#E6EDF3]">最新文章</h2>
      <a href="/blog/" class="text-[#58A6FF] hover:text-[#79C0FF] transition-colors">查看全部</a>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 lg:gap-8">
      {latestPosts.map((post) => (
        <a href={`/posts/${post.slug}/`} class="group block rounded-2xl border border-[#2A2D32] bg-[#0F1419] p-6 hover:border-[#58A6FF] hover:bg-[#1C2128] transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[0_12px_30px_rgba(88,166,255,0.15)] focus:outline-none focus:ring-4 focus:ring-[#58A6FF]/20">
          <h3 class="text-lg sm:text-xl font-semibold text-[#E6EDF3] group-hover:text-[#79C0FF] transition-colors">{post.data.title}</h3>
          <p class="mt-2 text-sm text-[#8B949E] overflow-hidden text-ellipsis" style="display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;">{post.data.description}</p>
          <div class="mt-5 flex items-center justify-between text-sm">
            <span class="text-[#8B949E]">{new Date(post.data.pubDate).toLocaleDateString('zh-TW', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
            <span class="text-[#58A6FF] group-hover:translate-x-0.5 transition-transform">閱讀
              <svg class="inline h-4 w-4 ml-1" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M5 12h14M13 5l7 7-7 7" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          </div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .chat-messages::-webkit-scrollbar {
    width: 8px;
  }

  .chat-messages::-webkit-scrollbar-track {
    background: #0F1419;
  }

  .chat-messages::-webkit-scrollbar-thumb {
    background: #2A2D32;
    border-radius: 4px;
  }

  .chat-messages::-webkit-scrollbar-thumb:hover {
    background: #58A6FF;
  }
</style>
